using System.Windows.Controls;

namespace MorrowindRemasteredLauncher.Views;

/// <summary>The Settings page; renders the per-edition setting rows from the view-model.</summary>
public partial class SettingsPanel : UserControl
{
    /// <summary>Initializes the panel.</summary>
    public SettingsPanel() => InitializeComponent();
}
